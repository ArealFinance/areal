# === Build artifacts ===
target/
dist/
build/
.svelte-kit/

# === Dependencies ===
node_modules/

# === Environment & secrets ===
.env
.env.*
!.env.example
*.pem
*.key

# === Internal planning (tracked separately in ArealFinance/areal-planning) ===
plan/
MIGRATION-PLAN.md

# === OS files ===
.DS_Store
Thumbs.db

# === IDE ===
.vscode/
.idea/
*.swp
*.swo

# === Archive (old Anchor code backup — DO NOT INCLUDE) ===
blockchain.zip

# === Claude local config (per-machine) ===
CLAUDE.local.md
.claude/settings.local.json

# === Rust ===
*.so
*.dylib

# === Logs ===
*.log
npm-debug.log*
deploy-keypair.json

# Vanity keypairs (private keys)
keys/

# === E2E bootstrap artifacts (Layer 9 Substep 12) ===
data/
data/e2e-bootstrap.secrets.json
data/cu-profile-*.json
data/e2e-runner-*.json
bots/*/data/
bots/*/.env
bots/merkle-publisher/local-mock-keypair.json

# === Merkle Publisher bot runtime state ===
bots/merkle-publisher/data/
bots/merkle-publisher/proofs/
bots/merkle-publisher/local-mock-keypair.json
bots/merkle-publisher/*.db
bots/merkle-publisher/*.db-journal
bots/merkle-publisher/*.db-wal
bots/merkle-publisher/*.db-shm
